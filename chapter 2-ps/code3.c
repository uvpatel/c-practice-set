//3. Write a program to check whether a number is divisible by 97 or not.

#include<stdio.h>
int main(){
    int a = 97.0,b; 
    printf("Enter number: ");
    scanf("%d", &b);
    printf("The division is:%d",a/b);
    return 0;
}