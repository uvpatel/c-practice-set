// temprature converter  cesius to fahrenthight

#include <stdio.h>

float convet(float celsius);

int main(){
    float celsius;
    printf("Enter celsius: ");
    scanf("%f",&celsius);
    printf("The fahrenhight of given celsius: %f",(9*celsius)/5 + 32);
    return 0;
    
    
}

float convert(float celsius){
    return convert(celsius);

}